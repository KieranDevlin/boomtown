const styles = theme => ({
  root: {
    width: '33%',
    backgroundColor: 'blue'
  },
  formControl: {
    margin: theme.spacing(1),
    width: '100%',
    margin: 0
  }
});

export default styles;
